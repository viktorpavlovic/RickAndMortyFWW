import React from "react";

const Pagination = () => {
  return (
    <div className="div-pagination">
      <button>left</button>
      <button>right</button>
    </div>
  );
};

export default Pagination;
